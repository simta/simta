domain "binary.example.com" {
    rule {
        type = accept;
        receive { enabled = true }
    }
    deliver local {
        enabled = true;
    }
}

domain "smtpd.example.com" {
    rule {
        type = accept;
        receive { enabled = true }
    }
}
