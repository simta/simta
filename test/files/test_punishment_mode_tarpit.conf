receive {
    mode = normal
    punishment = tarpit
    rcpt_to {
        max_failures = 2
    }
}
domain "example.com" {
    rule [
        {
            type = accept
            receive { enabled = true }
        }
    ]
}
