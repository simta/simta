receive connection {
    acl [
        {
            action = block;
            list = differ.dnsl.test
            message = default message
        }
        {
            action = block;
            list = normal.dnsl.test
            message = unused message
        }
    ]
}
