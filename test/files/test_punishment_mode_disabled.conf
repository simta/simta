receive {
    mode = normal
    punishment = disabled
    rcpt_to {
        max_failures = 2
    }
}
domain "example.com" {
    rule [
        {
            type = accept
            receive { enabled = true }
        }
    ]
}
